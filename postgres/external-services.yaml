---
# ExternalName service so apps can access the database as just "postgresql:5432"
apiVersion: v1
kind: Service
metadata:
  name: postgresql
  namespace: apps
spec:
  type: ExternalName
  externalName: postgresql-rw.databases.svc.cluster.local
