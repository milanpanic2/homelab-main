---
# ConfigMap with pgAdmin server configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-config
  namespace: databases
data:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "PostgreSQL",
          "Group": "Base service",
          "Host": "postgresql-rw",
          "Port": 5432,
          "MaintenanceDB": "postgres",
          "Username": "cluster_user",
          "SSLMode": "prefer"
        }
      }
    }

---
# pgAdmin Deployment - Web-based PostgreSQL management tool
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pgadmin
  namespace: databases
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pgadmin
  template:
    metadata:
      labels:
        app: pgadmin
    spec:
      containers:
      - name: pgadmin
        image: dpage/pgadmin4:latest
        ports:
        - containerPort: 80
          name: http
        env:
        - name: PGADMIN_DEFAULT_EMAIL
          value: homelab@dev.com
        - name: PGADMIN_DEFAULT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secret
              key: password
        - name: PGADMIN_CONFIG_SERVER_MODE
          value: "False"
        - name: PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED
          value: "False"
        - name: PGADMIN_SERVER_JSON_FILE
          value: /pgadmin4/servers.json
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        volumeMounts:
        - name: pgadmin-data
          mountPath: /var/lib/pgadmin
        - name: pgadmin-config
          mountPath: /pgadmin4/servers.json
          subPath: servers.json
      volumes:
      - name: pgadmin-data
        emptyDir: {}
      - name: pgadmin-config
        configMap:
          name: pgadmin-config

---
apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  namespace: databases
spec:
  selector:
    app: pgadmin
  ports:
  - port: 80
    targetPort: 80
    name: http

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pgadmin
  namespace: databases
  annotations:
    cert-manager.io/cluster-issuer: homelab-ca-issuer
spec:
  ingressClassName: traefik
  tls:
  - secretName: pgadmin-tls
    hosts:
    - sql.homelab.com
  rules:
  - host: sql.homelab.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pgadmin
            port:
              number: 80
